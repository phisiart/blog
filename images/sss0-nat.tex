\documentclass[crop]{standalone} %%[preview]
\usepackage{proof}
\usepackage{relsize}
\begin{document}
\relscale{1.5}
$\displaystyle
\infer[\texttt{nat} I_s]{
    s(s(s(0))) : \texttt{nat}
}{
    \infer[\texttt{nat} I_s]{
        s(s(0)) : \texttt{nat}
    }{
        \infer[\texttt{nat} I_s]{
            s(0) : \texttt{nat}
        }{
            \infer[\texttt{nat} I_0]{
                0 : \texttt{nat}
            }{}
        }
    }
}
$
\end{document}