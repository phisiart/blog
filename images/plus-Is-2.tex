\documentclass[crop]{standalone} %%[preview]
\usepackage{proof}
\usepackage{relsize}
\begin{document}
\relscale{1.5}
$\displaystyle
\infer[\texttt{plus} I_s]{
    \texttt{plus}(s(M'), N, s(P'))
}{
    \texttt{plus}(M', N, P')
}
$
\end{document}